// holds global constants which defines the default behaviour of concept of generation works for pokemons

const SECONDS = 1000;
const MINUTES = SECONDS * 60;
const HOURS = MINUTES * 60;
const DAYS = HOURS * 24;
const STARTING_BALANCE = 50;

// the value of refresh rate for generation is unit 
const REFRESH_RATE = 5; 

module.exports = {
    SECONDS,
    MINUTES,
    HOURS,
    DAYS,
    REFRESH_RATE,
    STARTING_BALANCE
};